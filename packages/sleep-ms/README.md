# @thinice/sleep-ms

await sleep(123) — everyone writes this.

## Installation

```bash
npm install @thinice/sleep-ms
```

## Usage

```typescript
import { sleepMs } from "@thinice/sleep-ms";

await sleepMs(1000); // Wait 1 second
await sleepMs(500);  // Wait 500ms
```
